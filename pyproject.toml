[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ploidy"
version = "0.3.3"
description = "Cross-session multi-agent debate MCP server. Same model, different context depths, better decisions."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "heznpc" },
]
keywords = ["mcp", "multi-agent", "debate", "llm", "context-asymmetry"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp>=1.0,<3",
    "aiosqlite>=0.20,<1",
]

[project.scripts]
ploidy = "ploidy.server:main"
ploidy-dashboard = "ploidy.dashboard:main"
ploidy-retention = "ploidy.retention:main"
ploidy-ask = "ploidy.cli:main"
ploidy-history = "ploidy.history_cli:main"

[project.optional-dependencies]
api = [
    "openai>=1.0",
]
dashboard = [
    "uvicorn>=0.30",
]
analysis = [
    "numpy>=1.24",
    "scipy>=1.10",
]
metrics = [
    "prometheus-client>=0.20",
]
redis = [
    "redis>=5.0",
]
cli = [
    "httpx>=0.27",
]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "httpx",
    "ruff",
    "prometheus-client>=0.20",
    "fakeredis>=2.20",
]
docs = [
    "mkdocs-material",
    "mkdocs-minify-plugin",
]
all = [
    "ploidy[api,dashboard,dev,docs,analysis,metrics,redis,cli]",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.ruff.lint.per-file-ignores]
"experiments/*" = ["E501"]  # experiment data contains long ground-truth strings

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "slow: end-to-end tests that spawn a real subprocess (deselect with '-m \"not slow\"')",
]
